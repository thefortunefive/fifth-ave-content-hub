{
  "name": "fifth-ave-dashboard",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node dist/server.js",
    "build": "vite build && npx esbuild src/server.ts --bundle --platform=node --format=esm --outfile=dist/server.js --external:@hono/node-server",
    "start": "node dist/server.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.0",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@hono/vite-cloudflare-pages": "^0.4.2",
    "vite": "^5.0.0",
    "wrangler": "^4.80.0"
  }
}
